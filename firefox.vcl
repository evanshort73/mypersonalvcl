# Voice commands for firefox
<n> := 0..100;

tack <n> = {ctrl+$1};
tack tar = {ctrl+tab};
tack tar <n> = {ctrl+tab_$1};
tack lap = {ctrl+shift+tab};
tack lap <n> = {ctrl+shift+tab_$1};
tack new = {ctrl+t};
tack restore = {ctrl+shift+t};
venture = {ctrl+l};
refresh = {ctrl+r};
